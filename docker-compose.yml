name: drift-king-miro
services:
  app:
    build: .
    ports:
      - "36988:80"
    restart: always
    healthcheck:
      test: [ "CMD-SHELL", "wget -q --spider http://127.0.0.1/" ]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s
